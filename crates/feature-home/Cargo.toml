[package]
name = "openspace-feature-home"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true
description = "Home shell that hosts the three first-class modes (terminal, chat, editor)."

[dependencies]
openspace-observability = { path = "../observability" }

[dev-dependencies]
tokio = { workspace = true }

[lints]
workspace = true
