//! Welcome mode crate.
//!
//! The welcome surface a user sees on first launch — workspace picker,
//! recent projects, onboarding tips. Implementation lands in a later
//! slice; this skeleton only ensures the crate compiles and tests run.

pub mod placeholder {}

#[cfg(test)]
mod tests {
    #[test]
    fn smoke() {}
}
