//! AI-specific error enum — `AiError`.
//!
//! Lives next to the provider trait rather than in the crate-level
//! `error` module because only the AI slice surfaces it. Populated in
//! a later PRD-01 slice.
